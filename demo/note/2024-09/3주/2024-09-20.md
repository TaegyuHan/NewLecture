남이 만들어놓은 html파일을 열지 말아라.

그러면 스크립트 실행 권한을 그대로 상속 받아서 실행이 된다.

eval의 코드를 실행하면 그 코드는 eval의 권한을 상속 받아서 실행된다.
그래서 eval을 사용하지 않는다.

인터넷이 보안의 허점이 많다.
그래서 보안을 강화하는 것이 중요하다.

네트워크에서 오는 데이터는 문자열이다.

연산자
자바 스크립트는 모두 값비교이다.
== : 값만 비교
=== : 값과 타입을 비교

자바 스크립트는 함수 정의가 없다.
그냥 쉽게 쓰겠금 만들어 놓았다.

자바 스크립트는 정의라는 개념이 없다.

언어를 오래 배우는 것은 안좋다. 
플랫폼과 같이 배우는 것이 좋다.

브라우저 개발자도구에서 객체는 마지막 상태가 나와서
흐름을 디버깅 하기 힘들 수 있다.